info:
  name: Event GET Attendees
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/events/1/attendees"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
