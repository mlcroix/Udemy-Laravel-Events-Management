info:
  name: Event
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/events/13?include=user,attendees,attendees.user"
  params:
    - name: include
      value: user,attendees,attendees.user
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
